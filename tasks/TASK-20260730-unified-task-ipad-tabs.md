# TASK-20260730：任务打卡与 iPad 使用同页 Tab

## 状态

已完成

## 来源规格

- `specs/features/unified-task-ipad-tabs.md`

## 目标

按照已确认的视觉稿 v2，将任务打卡与 iPad 使用整合到同一页面，并完成手机端适配。

## 范围

- 修改页面结构、视图切换、导航样式、响应式布局和缓存版本。
- 保持现有任务与 iPad 数据、领域规则和 Supabase 契约不变。
- 不部署、不执行数据库迁移。

## 验收映射

| 验收标准 | 实现 | 验证 |
| --- | --- | --- |
| AC-1、AC-2、AC-3 | 双 Tab 与 `showWorkspace()` | UI 与脚本契约测试、浏览器切换 |
| AC-4、AC-5 | 工作区专属工具栏 | DOM 归属测试 |
| AC-6、AC-7 | 独立激励条与响应式 CSS | CSS 契约测试、三视口检查 |
| AC-8 | `ipadMember` 初始化兼容 | 脚本契约测试、旧链接检查 |
| AC-9 | 保留既有模块与数据契约 | 完整 `node --test` |

## 工作清单

- [x] 已确认视觉稿与功能范围。
- [x] 已记录规格和实施计划。
- [x] 先编写并运行失败测试。
- [x] 实现同页 Tab 与响应式结构。
- [x] 运行聚焦测试和完整测试。
- [x] 完成浏览器验收与差异审查。

## 验证记录

- `node --check src/app.js`：通过。
- `node --test`：95 项测试全部通过。
- `git diff --check`：通过。
- 375px、768px、1280px 浏览器视口：无横向溢出。
- 三种视口均可在当前页面切换 Tab，`aria-selected` 与两个工作区的可见状态同步更新。
